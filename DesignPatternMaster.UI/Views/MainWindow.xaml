<Window x:Class="DesignPatternMaster.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:DesignPatternMaster.UI.ViewModels"
        mc:Ignorable="d"
        Title="{Binding ApplicationTitle}"
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/Assets/app_icon.png">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" 
                      GlassFrameThickness="0" 
                      CornerRadius="0" 
                      ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Background="{StaticResource SidebarBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon/Title -->
                <StackPanel Orientation="Horizontal" Margin="10,0" VerticalAlignment="Center">
                    <TextBlock Text="💠" Margin="0,0,8,0" Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text="{Binding ApplicationTitle}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                </StackPanel>

                <!-- Window Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Style="{StaticResource WindowCaptionButton}" Content="─" Click="MinimizeButton_Click" AutomationProperties.Name="最小化"/>
                    <Button x:Name="MaximizeButton" Style="{StaticResource WindowCaptionButton}" Content="⬜" Click="MaximizeButton_Click" AutomationProperties.Name="最大化"/>
                    <Button Content="✕" Click="CloseButton_Click" AutomationProperties.Name="閉じる">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource WindowCaptionButton}">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Main Layout -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                    <StackPanel Margin="10">
                        <TextBlock Text="Design Pattern Master" 
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="10,20,10,30"
                                   TextAlignment="Center"/>
                        
                        <Button Height="48"
                            Style="{StaticResource NavButton}"
                            HorizontalContentAlignment="Left"
                            Margin="0,0,0,5"
                            Command="{Binding NavigateCommand}" 
                            CommandParameter="Dashboard"
                            HorizontalAlignment="Stretch"
                            AutomationProperties.Name="Dashboardに移動"
                            ToolTip="Dashboardに移動">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" Margin="0,0,8,0" FontSize="18" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                                <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Height="48"
                            Style="{StaticResource NavButton}"
                            HorizontalContentAlignment="Left"
                            Margin="0,0,0,5"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Settings"
                            AutomationProperties.Name="設定に移動"
                            ToolTip="設定に移動">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" Margin="0,0,8,0" FontSize="18" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                                <TextBlock Text="Settings" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Content -->
                <Frame x:Name="RootFrame" 
                       Grid.Column="1" 
                       Margin="24"
                       NavigationUIVisibility="Hidden"
                       Background="{StaticResource BackgroundBrush}"/>
            </Grid>
        </Grid>
    </Border>
</Window>
