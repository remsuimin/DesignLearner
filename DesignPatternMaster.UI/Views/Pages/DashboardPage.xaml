<Page x:Class="DesignPatternMaster.UI.Views.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModels="clr-namespace:DesignPatternMaster.UI.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DashboardPage"
      Background="{StaticResource BackgroundBrush}">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>

    <Grid Margin="40,30,40,30" Style="{StaticResource PageEntranceStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="デザインパターン" Style="{StaticResource Heading1}"/>
            <TextBlock Text="モダンなC#デザインパターンをインタラクティブな例で習得しましょう。" 
                       Style="{StaticResource SecondaryText}" 
                       FontSize="16"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewer}">
            <ItemsControl ItemsSource="{Binding Patterns}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="320" Height="200" Margin="0,0,20,20" 
                                Background="{StaticResource SurfaceBrush}" 
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="12" 
                                Padding="20">
                            <Border.Resources>
                                <!-- Scoped text style fallback to ensure text becomes primary when card hovered -->
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
                                    <TextBlock Text="🧩" FontSize="24" Margin="0,0,12,0" Foreground="{StaticResource AccentBrush}"/>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource Heading3}" Margin="0"/>
                                </StackPanel>
                                
                                <TextBlock Grid.Row="1" 
                                          Text="{Binding Summary}" 
                                          TextWrapping="Wrap" 
                                          Style="{StaticResource SecondaryText}"
                                          LineHeight="20"/>
                                
                                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
                                    <Border Background="{StaticResource PrimaryBrush}" 
                                            CornerRadius="6" 
                                            Padding="10,4" 
                                            Margin="0,0,8,0">
                                        <TextBlock Text="{Binding Category}" 
                                                  Foreground="{StaticResource TextPrimaryBrush}"
                                                  FontSize="12" 
                                                  FontWeight="Medium"/>
                                    </Border>
                                    <Border Background="{StaticResource SurfaceHoverBrush}" 
                                            CornerRadius="6" 
                                            Padding="10,4">
                                        <TextBlock Text="{Binding Difficulty}" 
                                                  FontSize="12" 
                                                  Foreground="{StaticResource TextSecondaryBrush}"
                                                  FontWeight="Medium"/>
                                    </Border>
                                </StackPanel>
                                
                    <!-- Button overlay for command (use plain overlay style to avoid hover background changes) -->
                    <Button Grid.RowSpan="3" 
                        Background="Transparent" 
                        BorderThickness="0" 
                        Cursor="Hand"
                        Style="{StaticResource OverlayButton}"
                        Command="{Binding DataContext.NavigateToDetailCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
